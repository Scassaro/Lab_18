<% include header %>
   <% if(rs.length > 0) { %>
      <table>
	<tr><th>Model</th></tr>
	<% for (var i=0; rs.length > i; i++) { %>
	   <tr>
	   	<td><% rs %></a></td>
	   </tr>
	<% } %>
	</table>
	<% } else { %>
	   No users are entered into the database.
	<% } %>
     
<% include footer %>